function PolyEq = getPolyEqs(x, npoly, ndiv, nseg)
    % create an matrix for polynomical equations, each column corresponds
    % to a segement, row 1 original polynomical, row 2: 1st order
    % derivative ...

    sym_coefs = sym('c%d%d' [8 4]);
    
end % get PolyEqs

